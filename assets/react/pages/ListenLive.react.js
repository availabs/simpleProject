"use strict"
var React = require("react");

var ListenLive = React.createClass({

    render() {
        return (
            <div className="container main" style={{paddingTop:'100px'}}>
                Listen Live
            </div>
        );
    }
});

module.exports = ListenLive;
